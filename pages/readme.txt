该项目是一个角落里app对应的h5页面
###1.
    点击某一天: A:（add)
                1.放到数组里, clone_selectDate 是待操作的数据数组
                2.判断日期：选了一个还是多个，如果多个都'清白'，则可
                以多选，否则给出提示，且数组中不放该数据
               B:（reduce)
                1: